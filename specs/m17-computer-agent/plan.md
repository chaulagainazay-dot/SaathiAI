# M17 Plan
saathi/computer_agent/{perception,providers,operations,verification(in adapter),
replay,agent,cli}.py. operations.register() adds vision/desktop/browser_agent
connectors + tools (explicit risk) to the M15 registry with a ComputerAdapter.
Control Center Computer Center cell + /api/v1/control/computer. Red-team +5 probes.
Tests test_m17_computer_agent.py. Critical/route manifest m17. Local commits.
