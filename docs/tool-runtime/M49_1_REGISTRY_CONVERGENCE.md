# M49.1 Registry Convergence

Canonical: `saathi.tool_runtime.registry.ToolRegistry`.

Rules: unique tool_id@version; complete manifest; adapter required; dynamic registration
disabled by default; sealable; user input cannot register.

Legacy registries (saathi.tools, connectors.platform) remain for deferred domains.
