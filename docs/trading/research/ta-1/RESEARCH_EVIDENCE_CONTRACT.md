# Research evidence contract

Evidence is immutable-by-reference metadata. Claims are validated only from evidence visible at decision time (`available_at <= T`) and non-invalid quality. Missing/future evidence yields `INSUFFICIENT_EVIDENCE`.
