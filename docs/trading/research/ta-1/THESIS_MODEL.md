# Thesis model

`StructuredInvestmentThesis` aggregates supporting/contradicting claims for one instrument and expires at `valid_until`. It contains no order, approval, sizing, broker, or risk override fields.
