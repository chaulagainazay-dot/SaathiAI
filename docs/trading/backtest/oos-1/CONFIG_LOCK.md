Validation selects an immutable `LockedStrategyConfiguration` with a SHA-256
parameter hash, selection datasets, trial count, cost model, and fill model.
Post-test mutation requires a new evaluation.
