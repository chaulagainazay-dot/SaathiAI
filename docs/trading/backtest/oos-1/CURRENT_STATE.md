# BACKTEST-OOS-1

The canonical OOS contracts are now represented by `StrategyEvaluationPlan`,
`LockedStrategyConfiguration`, `WalkForwardResult`, and visibility validation.
Existing M62.4 walk-forward execution remains the runtime evaluator.
