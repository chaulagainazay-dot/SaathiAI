# Limitations

The paper adapter has no external network venue, so its external snapshot is a
separate paper projection. External broker sequence reconciliation and durable
multi-process locking remain future adapter responsibilities. Offline suite
hang investigation is tracked as TEST-INFRA-1.
