# Instrument mapping

Only allowlisted spot symbols (`BTCUSDT`, `ETHUSDT`, `BTCUSDC`, `ETHUSDC`) are accepted. Provider aliases (`BTC/USDT`, `BTC-USDT`) normalize to canonical `BINANCE:BTC/USDT`; aliases never become financial identity. Exchange metadata supplies base/quote assets and canonical identity validation rejects unrelated venues.
