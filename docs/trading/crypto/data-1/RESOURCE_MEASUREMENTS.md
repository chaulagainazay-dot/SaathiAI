# Resource measurements

Focused fixture tests complete in under one second and use bounded in-memory queues. REST responses are capped at 2 MiB; stream buffering is bounded by configuration (default 256 frames). Long-running retention, CPU/RAM/network measurement and persistent supervision are deferred to CRYPTO-DATA-2.
