# Live probe

One bounded unauthenticated GET to the official Binance public ticker endpoint was attempted (2026-09-01, 5 s timeout, no credentials). The environment could not resolve the provider host (`URLError: nodename nor servname provided`), so no response was obtained. This is an environment/network limitation, not evidence of provider availability; no retry loop was started.
