# Point-in-time analysis

Future adapters must populate MD-1 `as_of`, `available_at`, and `received_at`. Public portal content exposes trade-date context but no verified publication timestamp; availability precision must therefore remain uncertain. Licensed providers must document timezone, delay, timestamp precision, and historical availability.
