# Resource model

Daily NEPSE OHLCV is compact and fits existing dataset storage. Tick/intraday archives, redundant raw copies, and new database engines are out of scope; retention is manifest/checksum driven.
