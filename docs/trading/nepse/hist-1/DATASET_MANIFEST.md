# Dataset manifest

`DatasetManifest` records stable dataset id/version, source reference, checksum, calendar/schema versions, availability precision, and certification status. Revisions receive new versions; certified content is never overwritten silently.
