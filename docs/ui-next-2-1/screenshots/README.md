# Screenshots

Automated Playwright capture was not run in-session (may require next dev + browser install).

Interactive verification targets:
- Command desktop (healthy / risk_warning / recon_required)
- Agents / Investments / Evidence modes
- Mobile stack
- Voice LISTENING/SPEAKING
- prefers-reduced-motion

Capture command (operator):

```bash
cd saathi-os && npm run dev
# open /design-lab and use Scenario select + modes
```
