# OWNER_CORPUS_REPORT

## Status

```text
OWNER LIVE RECORDING: PENDING
```

Owner natural speech was **not** auto-filled. Protocol:

- `tools/voice-stt-bench/corpus/owner_real/README.md`
- Preferred storage: `~/.saathi/stt-owner-corpus/` (git-excluded WAVs)
- Metrics only committed after owner run

## Why still valid to decide

Locked TTS corpus already fails gate with large margin on specialized models. Owner speech is still **required** before any future product-primary flip even if a future model passes TTS gate.

