# REDUCED_MOTION_REPORT

Playwright context `reducedMotion: reduce` + screenshot `12-reduced-motion.png`.

CSS:

```css
@media (prefers-reduced-motion: reduce) { animation: none }
html.dl-reduced-root .dl-orb ... animation none
```

Essential state remains via text labels (VOICE pill, transcript).

