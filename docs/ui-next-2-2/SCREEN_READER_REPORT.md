# SCREEN_READER_REPORT

Automated tree not fully exercised with VoiceOver.

Accessible names present for:
- tabs (`role=tab`)
- agent buttons (`aria-label`)
- position buttons (`aria-label`)
- Saathi orb (`aria-label`)
- recon banner (`role=alert`)
- demo banner (`role=status`)

**Limitation:** no full VoiceOver session log.

