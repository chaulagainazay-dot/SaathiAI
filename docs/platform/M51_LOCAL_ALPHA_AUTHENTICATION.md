# M51 Local Alpha Authentication

- Secure bootstrap requires password.
- Login uses generic `auth_failed` responses.
- Recovery codes: `PRIVATE_ALPHA_ONLY` / `NOT_PRODUCTION_RECOVERY` (not emailed).
- M50 passwordless login retained for empty-credential users only.
