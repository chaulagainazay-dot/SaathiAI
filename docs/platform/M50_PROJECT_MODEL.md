# M50 Project Model

## Fields

project_id, workspace_id, org_id, name, owner_id, mission_key, status, timestamps

## Ownership

Created by operator+; `owner_id` set to creating user.

## Execution

Optional `project_id` on execute context; approvals can bind project scope.

## State

`PROJECT_MODEL_ACTIVE`
