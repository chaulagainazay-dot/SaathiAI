# M51 Organization Invitations

Lifecycle: created/pending → accepted | expired | revoked | rejected.

Single-use token hash. Role cannot exceed inviter rank. System role not grantable.
LOCAL_PRIVATE_ALPHA_INVITE codes are copyable, not emailed.
