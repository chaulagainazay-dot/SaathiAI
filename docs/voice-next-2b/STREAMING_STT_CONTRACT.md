# STREAMING_STT_CONTRACT

```text
start / pushAudio? / onPartial / onFinal / flush / cancel / close / health / capabilities
```

TranscriptEvent: sessionId, utteranceId, text, isFinal, confidence, language, timestamps, sequence, source, privacyClass.

**PARTIAL ≠ EXECUTABLE INTENT**
