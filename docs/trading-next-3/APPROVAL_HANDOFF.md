# APPROVAL_HANDOFF

`approval_handoff_payload(proposal_id)` returns:

proposal_id, summary, proposed_trades, current_vs_proposed, risk_result, warnings, expiry, evidence_references,
authorizes_execution=false, mode=PAPER.

Only when status is READY_FOR_APPROVAL. No auto-approve. No auto-submit.

