# EVIDENCE_MODE

Causal timeline:

```text
research → proposal → risk → TG → approval → paper order → fill →
ledger → reconciliation → portfolio change
```

Each node links to evidence IDs when available. Unknown fields stay unknown.

