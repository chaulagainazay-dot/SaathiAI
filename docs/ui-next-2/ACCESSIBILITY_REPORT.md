# ACCESSIBILITY_REPORT

Prototype `/design-lab` requirements:

| Check | Approach |
| --- | --- |
| Contrast | Graphite surfaces + light text tokens; status colors not sole channel |
| Keyboard | Mode tabs, focus rings, composer focusable |
| Screen reader | Landmarks, aria-labels, live regions for voice state |
| Reduced motion | CSS `prefers-reduced-motion` disables pulse |
| Zoom | Fluid grid, no fixed tiny hit targets |
| Widths | 320 / 768 / 1280 breakpoints in CSS |

Production `/command` unchanged for this milestone.

