# PORTFOLIO_AUTHORITY

```text
PortfolioLedgerService
  create_fund / register_security
  record_deposit / record_withdrawal_sim
  record_fill / record_fee / record_adjustment / record_mark
  reverse_event
  get_cash / get_positions / get_lots / get_nav / get_pnl / get_exposure
  replay / snapshot / reconcile
  command_center_summary
```

Forbidden: `set_position`, `set_nav`, `set_pnl`.

