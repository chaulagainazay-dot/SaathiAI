# ATTENTION_MODEL

Reuses `lib/attention.js` `aggregateAttention` sources:

- control attention
- missions (blocked/failed)
- pending approvals
- infra degraded
- evidence ready

Each item surfaces: what, category (why), authority, source, evidence id if any, action route.

No new authority store.
