# M48.3 — Provider Health

`provider_health_evidence()`:

- Ollama GET 127.0.0.1:11434/api/tags (1.5s, local only)
- model_router module presence
- remote_paid_providers = PROHIBITED (not probed)

CLI: `lifecycle-health`.
