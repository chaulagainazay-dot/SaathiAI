# M48.4 — Memory Compatibility

M8 wrap uses Orchestrator memory=False for isolation in tests; production default_orchestrator may attach M9. Scope cannot widen via force_agent. Secrets not added to prompts by bridge.
