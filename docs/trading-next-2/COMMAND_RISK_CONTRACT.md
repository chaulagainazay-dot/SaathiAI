# COMMAND_RISK_CONTRACT

`PortfolioRiskEngine.command_risk_contract` / `PaperTradingService.paper_risk_snapshot`:

```text
label: PAPER RISK
mode: PAPER
live_execution: UNAVAILABLE
risk_status, nav, drawdown, daily/weekly pnl,
gross exposure, cash %, largest position,
risk_budget_consumed bars, active_breaches, stress_loss
```

Command composition pass-through via `summary.paper_risk`.

